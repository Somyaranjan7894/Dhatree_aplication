services:
  - type: web
    name: crop-recommendation-system
    env: python
    rootDir: .
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: cd Crop-Recommendation-System-Using-Machine-Learning-main && gunicorn app:app
